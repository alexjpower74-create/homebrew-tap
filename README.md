# Homebrew tap for Alexander Power's tools

```sh
brew tap alexjpower74-create/tap
brew install rig        # orchestration for several Claude Code sessions on one repo
brew install sightline  # small-business website audits that never invent a number
```

Formulas install from the tagged GitHub releases of each project. Both need Node 22 or newer, which Homebrew brings in.
